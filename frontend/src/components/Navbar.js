import { MdMenu } from "react-icons/md";
const Navbar = () => {
    return (
        <header>
            <h1 style={{ cursor: "default" }}>Work manager</h1>
            <MdMenu style={{ fontSize: 36, cursor: "pointer" }} />
        </header>
    )
}

export default Navbar;